name: screenlantern-dev

services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: screenlantern
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - screenlantern_dev_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d screenlantern"]
      interval: 5s
      timeout: 5s
      retries: 20

  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/screenlantern?schema=public
      NEXTAUTH_URL: http://localhost:3000
      AUTH_TRUST_HOST: "true"
      PORT: "3000"
      SEED_DEMO_DATA: "1"
      SEED_DEMO_MODE: "full"
      STREAMING_SYNC_URL: http://streaming-sync:7331
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - screenlantern_dev_node_modules:/app/node_modules
      - screenlantern_dev_next:/app/.next
    command: ["/bin/sh", "./scripts/start-dev.sh"]

  streaming-sync:
    build:
      context: .
      dockerfile: Dockerfile.netflix-sync
    restart: unless-stopped
    depends_on:
      - app
    env_file:
      - .env
    environment:
      SCREENLANTERN_INTERNAL_URL: http://app:3000
      NETFLIX_SYNC_USER_DATA_DIR: /data/profile
      NETFLIX_SYNC_STORAGE_STATE_PATH: /data/storage-state.json
      NETFLIX_SYNC_DOWNLOAD_DIR: /data/downloads
      NETFLIX_SYNC_HEADLESS: "1"
      STREAMING_SYNC_PORT: "7331"
    volumes:
      - .:/app
      - screenlantern_dev_node_modules:/app/node_modules
      - screenlantern_dev_next:/app/.next
      - ./.netflix-sync:/data

volumes:
  screenlantern_dev_postgres_data:
  screenlantern_dev_node_modules:
  screenlantern_dev_next:
